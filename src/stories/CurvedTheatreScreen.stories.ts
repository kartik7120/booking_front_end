import { Meta, StoryObj } from "@storybook/react";
import CurvedTheatreScreen from "../components/MovieTimeSlots/CurvedTheatreScreen";

const meta = {
    title: "Seat Selection/Curved Theatre Screen",
    component: CurvedTheatreScreen,
} satisfies Meta<typeof CurvedTheatreScreen>;

export default meta;

type Story = StoryObj<typeof CurvedTheatreScreen>;

export const Primary: Story = {

}